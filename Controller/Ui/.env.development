# Use the real HTTP backend for local dev. Set to "memory" to force the in-memory fake.
# The backend's UI-compat surface is mounted under /api (see UiCompatEndpoints.cs), so the
# base URL must include that prefix.
VITE_BACKEND_MODE=http
VITE_BACKEND_URL=http://localhost:5145/api